// Empty placeholder so MSBuild has a source file to compile while the
// Payroll bounded context is being scaffolded. Delete once real types arrive.
namespace Karamchari.Payroll;

internal static class PlaceholderForBuild;
