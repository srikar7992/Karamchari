# WS11 — Observability Completion

**Status: ✅ CLOSED (was MEDIUM).**

## Changes implemented
1. Full stack in compose: `otel-collector`, `prometheus`, `grafana`, `seq` (+ durable volumes — see WS13).
2. **Fixed collector → Seq exporter**: was OTLP **gRPC** to `seq:5341` (`no children to pick from`); changed to OTLP **HTTP** (`otlphttp/seq` → `http://seq:5341/ingest/otlp`) in `otel-collector-config.yaml`.
3. **Fixed Seq startup with durable volume**: added `SEQ_FIRSTRUN_NOAUTHENTICATION=true` (Seq `:latest` requires first-run auth config when a persistent volume is mounted, else it exits 1).

## Verification (live, after generating auth + employee traffic)
- **Logs + Traces → Seq**: `GET /api/events?count=500` → **500 events ingested**; collector export errors in last interval: **0**.
- **Metrics → Prometheus**: scrape target `otel-collector:8889` health = **up**; collector `:8889` exposes real app metrics, e.g.
  `aspnetcore_rate_limiting_active_request_leases{...,aspnetcore_rate_limiting_policy="auth",service_name="...Karamchari.Api"}` plus `http_server_*` / `http_client_*`.
- App OTel config (`InfrastructureExtensions.cs`): tracing (AspNetCore, HttpClient, EF Core, MassTransit, `Karamchari.*` sources) + metrics, both `AddOtlpExporter` → `OTEL_EXPORTER_OTLP_ENDPOINT` (collector).
- Collector pipelines: traces+logs → Seq, metrics → Prometheus.

## Verdict
OpenTelemetry = **PASS** — logs, traces, and metrics generated by the app are observed arriving at Seq (logs/traces) and Prometheus (metrics).
